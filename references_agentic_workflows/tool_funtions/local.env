# Local development environment variables
# Fill these with local/test credentials. Do NOT commit sensitive values in shared repos.

# ClickHouse (local/test)
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=your_clickhouse_user
CLICKHOUSE_PASSWORD=your_clickhouse_password

# Postgres (local/test)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=your_postgres_user
POSTGRES_PASS=your_postgres_password
POSTGRES_DB_NAME=your_db_name

# Google Sheets (local)
# Preferred: set path to service account JSON file if you have one locally.
GOOGLE_SHEET_SERVICE_ACCOUNT_JSON_PATH=/path/to/your/service_account.json
# Alternatively, you can paste the JSON string directly (not recommended):
GOOGLE_SHEET_SERVICE_ACCOUNT=''

# Local logging
LOG_LEVEL=DEBUG

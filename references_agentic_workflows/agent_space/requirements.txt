# requirements.txt — curated set of runtime dependencies for the agentspaces project
# Use a virtual environment; pin or loosen versions as needed for your environment.

# Web / server
fastapi>=0.95.2
uvicorn[standard]>=0.22.0

# Data models
pydantic>=2.0.0
python-dotenv>=1.0.1

# LangChain and related adapters
langchain==1.0.1
langchain-core==1.0.0
langchain-google-genai==3.0.0
langchain-google-vertexai==3.0.0
langchain-community==0.4.0
langchain-mcp-adapters>=0.1.0

# ML / vector libs
faiss-cpu>=1.7.4

# Data / DB clients
pandas>=2.0.0
clickhouse-driver>=0.1.0
clickhouse-connect>=0.6.15
psycopg2-binary>=2.9.6

# HTTP client
requests>=2.31.0

# Notes:
# - If you have platform-specific constraints (GPU / macOS), adjust faiss-cpu and other binaries accordingly.
# - If a package above isn't available in your environment, try loosening the version or installing from source.
